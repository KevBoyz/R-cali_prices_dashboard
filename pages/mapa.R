# ── Mapa Interativo ────────────────────────────────────────────────────────
# Placeholder — funcionalidade a implementar (ex: leaflet com coordenadas do dataset).

mapa_ui <- function() {
  div(
    class = "stats-page",
    h2("Mapa Interativo"),
    p("Em desenvolvimento.")
  )
}

mapa_server <- function(input, output, session) {
  # Sem outputs por ora.
}
